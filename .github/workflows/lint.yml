name: Lint

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]

jobs:
  Eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Linters
        run: |
          npm install
          npm run eslint
